<b><%= l.timestamp %></b> (<%= l.mode %>, <i><%= l.area %></i>)
<%= if l.mode & 8 == 8
		 (l.character.nil? ? l.character_s : link_to_character(l.character, true)) + 
		# l.character_s
			" -> " + 
		l.tcharacter
	else
		# l.character_s
		l.character.nil? ? l.character_s : link_to_character(l.character, true)
end %>: <%= l.text %><br>
