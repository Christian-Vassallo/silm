extern("_gen")
extern("inc_mnx")
#include "inc_pgsql.nh"

void main() {
	object oPC = OBJECT_SELF;

	string slogan;

	pQ("select text from login_slogans order by random() limit 1;");
	if (pF()) {
		slogan = pGs(1);
	} else {
		slogan = "Willkommen!";
	}

	DelayCommand(10.0, FloatingTextStringOnCreature(slogan, oPC, FALSE));
}
