void main() {
	object clicker = GetClickingObject();

	if (GetIsOpen(OBJECT_SELF)) {
		AssignCommand(clicker, ActionCloseDoor(OBJECT_SELF));
	} else {
		AssignCommand(clicker, ActionOpenDoor(OBJECT_SELF));
	}
}

