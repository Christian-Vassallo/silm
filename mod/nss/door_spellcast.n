extern("inc_doors")

void main() {
	// Event: door_spellcast
	// - runnable: door
	RunEventScriptsFor(GetEventSerial(), "door_spellcast", OBJECT_SELF, OBJECT_INVALID, OBJECT_INVALID, GetLocation(OBJECT_INVALID));
}
