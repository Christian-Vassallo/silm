/*void Debug(string sMessage)
 * {
 * WriteTimestampedLogEntry(sMessage);
 * SendMessageToAllDMs(sMessage);
 * }
 *
 * void DebugPC(object oPC, string sMessage)
 * {
 * SendMessageToPC(oPC,sMessage);
 * Debug("DEBUG: "+GetName(oPC)+" - "+sMessage);
 * } */

